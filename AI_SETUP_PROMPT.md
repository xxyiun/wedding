# AI 一键定制提示词

你不需要看懂代码。在这个项目文件夹中打开 AI 编程工具（例如 Codex、Cursor、Claude Code），把下面的提示词整段复制进去，再补充你的婚礼信息即可。

> 提示：将 `【】` 内容换成自己的信息。不想提供的项目可以写“待确认”，让 AI 暂时保留占位文案。

```text
你正在修改一个星露谷像素风格的手机婚礼邀请函模板。请直接在当前项目中完成下面的定制，不要只告诉我步骤。

我的婚礼信息：
- 新郎中文姓名：【新郎姓名】
- 新娘中文姓名：【新娘姓名】
- 新郎英文名或拼音：【GROOM】
- 新娘英文名或拼音：【BRIDE】
- 婚礼日期（中国时间）：【2030年10月1日】
- 婚礼地点完整名称：【示例市幸福区星露谷宴会厅】
- 地图上显示的短名：【星露谷宴会厅】
- 签到时间：【14:00】
- 仪式时间：【15:00】
- 喜宴时间：【18:00】
- 合影时间：【待确认】
- 是否有已授权的背景音乐：【有 / 没有】

请严格按下列要求操作：

1. 为我修改 `app.js` 顶部的 `weddingConfig`：
   - 填入新人姓名、英文名、日期、地点、地图短名和四段日程。
   - 倒计时必须使用 `+08:00` 中国时区。
   - 自行计算星期、月份英文缩写、日和年，一并填入 `dateDot`、`dateCn`、`calendarMonth`、`calendarDay`、`calendarYear`。
   - 使用高德地图搜索链接生成 `navigationUrl`，保留 `src=stardew-wedding` 和 `callnative=1`。

2. 为我修改分享信息：
   - 同步更新 `index.html` 中的 `<title>`、`meta name="description"`、`og:title` 和 `og:description`。
   - 同步更新 `assets/share-cover.svg` 中的新人姓名与日期。

3. 不要更改项目的视觉风格、页面结构、像素素材、字体、角色图像或已有动效。不要新增报名、留言、后端、数据收集等功能。

4. 背景音乐处理：
   - 项目已提供默认 `assets/wedding-bgm.mp3`。如果我写“有”，提醒我用已授权的 MP3 替换该文件；不要下载或引用任何未授权的音乐。
   - 如果我写“没有”，保留默认音乐，不需改代码。iOS、Android 和微信通常需要访客点击右下角音乐按钮开始播放。

5. 修改后运行 `npm run build`。如果出错，自行修复后重新运行；如果成功，用简单中文告诉我：
   - 修改了哪些文件；
   - 地图导航会打开哪个地点；
   - 是否需要我放入背景音乐文件。
如果上面的信息都已填写完整，不需要等待我再次确认，请直接完成修改、构建和总结。只有在信息缺失、日期有歧义或地点不明确时，才提问。
```

## 如何使用

1. 把这个项目文件夹打开到你常用的 AI 编程工具。
2. 复制上方代码块中的全部内容。
3. 把 `【】` 中的示例替换为自己的婚礼信息。
4. 粘贴给 AI。信息填完整时，它会直接完成修改和构建；信息缺失时才会向你追问。
如果你只是在聊天式 AI（无法直接访问你文件的那种）中使用，请先将项目打包上传，或者使用能够直接打开本地文件夹的 AI 工具。
